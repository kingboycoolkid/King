---
description: Run the scrollcraft preflight (node, ffmpeg, Chrome, API key, workspace)
allowed-tools: Bash
---

Run `node .claude/skills/scrollcraft/scripts/doctor.mjs $ARGUMENTS` from the
project root and report the result. `--probe` spends one kie.ai API call to read
the credit balance. Never print the API key itself.
